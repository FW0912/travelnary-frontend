import { ValidatorFn } from "@angular/forms";

export class StringValidators {
	// public static validateNumber: ValidatorFn = (control) => {
	//     if(control.value === null || control.value === undefined || control.value === "") {
	//         return
	//     }
	// }
}
